# 🚗 Car Sharing Backend

![Java](https://img.shields.io/badge/Java-21-orange?logo=java)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.5.6-brightgreen?logo=spring)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-latest-blue?logo=postgresql)
![Test Coverage](https://img.shields.io/badge/Coverage-~50%25-yellow)
![Tests](https://img.shields.io/badge/Tests-807%2F1232%20passing-blue)
![Maven](https://img.shields.io/badge/Maven-3.x-red?logo=apache-maven)

## 📋 Описание проекта

**Car Sharing Backend** — это серверное приложение для системы каршеринга (аренды автомобилей). Проект предоставляет REST API для управления автомобилями, клиентами, договорами аренды и аналитикой.

### Основные возможности:
- 🔐 **Аутентификация и авторизация** (JWT токены, refresh tokens)
- 👤 **Управление пользователями** (регистрация, профиль, верификация email)
- 🚙 **Каталог автомобилей** (фильтрация по брендам, моделям, классам, годам)
- 📝 **Управление договорами** аренды (создание, отслеживание статусов)
- ⭐ **Избранное** (добавление автомобилей в избранное)
- 📊 **Аналитика** для администраторов
- 📄 **Управление документами** клиентов
- 📧 **Email-уведомления** (через Mailpit в dev-режиме)

### Зачем нужен проект?
Проект решает задачу автоматизации бизнес-процессов каршеринга: от регистрации пользователей до заключения договоров аренды и управления автопарком.

---

## 🛠 Технологии

### Backend
- **Java 21** — современная версия Java с новейшими возможностями
- **Spring Boot 3.5.6** — фреймворк для создания enterprise-приложений
  - Spring Web (REST API)
  - Spring Security (JWT аутентификация)
  - Spring Data JPA (работа с БД через ORM)
  - Spring Validation (валидация данных)
- **Hibernate** — ORM для работы с базой данных
- **MapStruct 1.6.3** — маппинг между DTO и Entity
- **Lombok** — сокращение boilerplate кода

### База данных
- **PostgreSQL** — реляционная СУБД
- **Flyway** — миграции БД (версионирование схемы)

### Безопасность
- **JWT (JSON Web Tokens)** — stateless аутентификация
- **JJWT 0.11.5** — библиотека для работы с JWT
- **BCrypt** — хеширование паролей

### Тестирование
- **JUnit 5** — unit и integration тесты
- **Testcontainers** — тестирование с реальной PostgreSQL в контейнере
- **JaCoCo** — измерение покрытия кода тестами
- **Spring Boot Test** — тестирование Spring-компонентов

### DevOps
- **Docker & Docker Compose** — контейнеризация приложения
- **Maven** — сборка проекта и управление зависимостями
- **Mailpit** — тестирование email-уведомлений в dev-режиме

---

## 🚀 Запуск проекта

### Требования
- **Java 21** (JDK)
- **Maven 3.x**
- **Docker** и **Docker Compose**
- **Git**

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd backend
```

### 2. Запуск через Docker Compose (рекомендуется)

#### Вариант A: Development режим
```bash
docker-compose -f docker-compose.dev.yml up -d
```
Этот вариант запускает:
- PostgreSQL на порту `5433`
- Mailpit (SMTP сервер для тестирования email) — интерфейс доступен на `http://localhost:8025`

#### Вариант B: Production режим
```bash
docker-compose up -d
```

#### Вариант C: С MinIO (для хранения файлов)
```bash
docker-compose -f docker-compose_miniIo.yml up -d
```

### 3. Запуск приложения

#### Через Maven
```bash
mvn clean install
mvn spring-boot:run
```

#### Через JAR
```bash
mvn clean package
java -jar target/backend-0.0.1-SNAPSHOT.jar
```

### 4. Проверка работы
API будет доступно по адресу: `http://localhost:8080/api`

---

## 📡 Примеры API

### Регистрация пользователя
```bash
curl -X POST http://localhost:8080/api/registration \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePass123!",
    "firstName": "Иван",
    "lastName": "Иванов",
    "phone": "+79991234567"
  }'
```

### Аутентификация
```bash
curl -X POST http://localhost:8080/api/auth \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePass123!"
  }'
```

Ответ:
```json
{
  "message": "Login successful"
}
```
Токены приходят в cookies: `access_token` и `refresh_token`

### Получение списка автомобилей (с фильтрацией)
```bash
curl -X GET "http://localhost:8080/api/car?brand=Toyota&minYear=2020&maxYear=2024&carClass=Economy&page=0&size=10" \
  -H "Authorization: Bearer <access_token>"
```

### Получение деталей автомобиля
```bash
curl -X GET http://localhost:8080/api/car/1 \
  -H "Authorization: Bearer <access_token>"
```

### Добавление автомобиля в избранное
```bash
curl -X POST http://localhost:8080/api/favorites/1 \
  -H "Authorization: Bearer <access_token>"
```

### Создание договора аренды
```bash
curl -X POST http://localhost:8080/api/contract \
  -H "Authorization: Bearer <access_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "carId": 1,
    "dateStart": "2024-12-10T10:00:00",
    "dateEnd": "2024-12-15T10:00:00",
    "comment": "Аренда для путешествия"
  }'
```

---

## 📁 Структура проекта

```
backend/
├── src/
│   ├── main/
│   │   ├── java/org/example/carshering/
│   │   │   ├── config/              # Конфигурация Spring (Security, CORS, etc.)
│   │   │   ├── domain/              # Доменная модель
│   │   │   │   ├── entity/          # JPA сущности
│   │   │   │   └── valueobject/     # Value Objects (DDD) - Money и т.д.
│   │   │   ├── dto/                 # Data Transfer Objects
│   │   │   │   ├── request/         # DTO для входящих запросов
│   │   │   │   └── response/        # DTO для ответов
│   │   │   ├── exceptions/          # Кастомные исключения
│   │   │   ├── mapper/              # MapStruct маперы (Entity ↔ DTO)
│   │   │   ├── repository/          # Репозитории для работы с БД
│   │   │   │   ├── impl/            # Кастомные реализации
│   │   │   │   └── interfaces/      # Интерфейсы репозиториев
│   │   │   ├── rest/                # REST контроллеры
│   │   │   │   ├── admin/           # Endpoints для администраторов
│   │   │   │   └── all/             # Публичные endpoints
│   │   │   ├── security/            # Настройки безопасности (JWT, UserDetails)
│   │   │   ├── service/             # Бизнес-логика
│   │   │   │   ├── impl/            # Реализации сервисов
│   │   │   │   └── interfaces/      # Интерфейсы сервисов
│   │   │   ├── utils/               # Вспомогательные утилиты
│   │   │   └── CarSheringApplication.java  # Главный класс
│   │   └── resources/
│   │       ├── application.yaml     # Конфигурация приложения
│   │       └── db/migration/        # Flyway миграции
│   └── test/                        # Тесты
│       ├── java/                    # Unit и Integration тесты
│       └── resources/               # Конфигурация для тестов
├── docker-compose.yaml              # Docker Compose для production
├── docker-compose.dev.yml           # Docker Compose для development
├── docker-compose_miniIo.yml        # Docker Compose с MinIO
├── pom.xml                          # Maven зависимости
├── MIGRATION_PLAN.md                # План миграции на DDD
└── README.md                        # Этот файл
```

### Архитектурные слои

1. **REST Layer** (`rest/`) — HTTP контроллеры, обработка запросов
2. **Service Layer** (`service/`) — бизнес-логика
3. **Repository Layer** (`repository/`) — доступ к данным (JPA)
4. **Domain Layer** (`domain/`) — доменные модели, сущности, value objects
5. **DTO Layer** (`dto/`) — объекты передачи данных
6. **Security Layer** (`security/`) — аутентификация и авторизация

---

## 🗂 Доменная модель (основные сущности)

- **Client** — клиент (пользователь системы)
- **Car** — автомобиль в каршеринге
- **Contract** — договор аренды
- **CarModel** — модель автомобиля (связь Brand + ModelName)
- **Brand** — бренд автомобиля (Toyota, BMW, etc.)
- **Model** — название модели (Camry, X5, etc.)
- **CarClass** — класс автомобиля (Economy, Business, Premium)
- **CarState** — состояние автомобиля (Available, Rented, Maintenance)
- **RentalState** — статус аренды (Active, Completed, Cancelled)
- **Document** — документы клиента (паспорт, водительское удостоверение)
- **Favorite** — избранные автомобили клиента
- **Role** — роль пользователя (USER, ADMIN)

### Value Objects (DDD)
- **Money** — денежное значение (сумма + валюта), используется в Contract

---

## 🔄 Миграция на DDD (Domain-Driven Design)

### Текущий статус миграции

Проект находится в процессе рефакторинга с традиционной архитектуры на **Domain-Driven Design (DDD)**.

#### ✅ Выполнено:
- [x] Создана структура папок `domain/valueobject/`
- [x] Реализован Value Object **Money** (для работы с денежными суммами в Contract)
- [x] Интеграция Money в сущность Contract через `@Embedded`

#### 🔄 В процессе:
- [ ] Создание остальных Value Objects (4/5):
  - [ ] **Email** (для валидации и работы с email)
  - [ ] **PhoneNumber** (валидация телефонов)
  - [ ] **VIN** (Vehicle Identification Number)
  - [ ] **LicensePlate** (государственный номер)

#### 📋 Запланировано:

**Этап 2: Contract Aggregate**
- Выделение бизнес-логики в аггрегат Contract
- Перенос валидации в доменный слой
- Создание доменных событий (ContractCreated, ContractCompleted)

**Этап 3: Car Aggregate**
- Инкапсуляция логики управления автомобилем
- Бизнес-правила проверки доступности авто
- Управление состоянием (State Pattern)

**Этап 4: Client Aggregate**
- Аггрегат для управления клиентами
- Бизнес-логика верификации
- Управление документами

**Этап 5: Repositories (DDD-стиль)**
- Перевод репозиториев на интерфейсы DDD
- Отделение инфраструктуры от доменного слоя

**Этап 6: Обновление тестов**
- Рефакторинг существующих тестов под новую архитектуру
- Повышение покрытия до 70-80%
- Тесты доменной логики (unit tests для аггрегатов)

### Проблемы при миграции
- **Тесты**: Часть тестов (425 из 1232) падает из-за рефакторинга методов
- **Покрытие**: Текущее покрытие ~50%, цель — 70-80%

### План работы с проектом для DDD-рефакторинга

#### Шаг 1: Изучение текущего кода
1. Прочитать `MIGRATION_PLAN.md` — план миграции
2. Изучить существующий Value Object `Money` в `domain/valueobject/`
3. Посмотреть, как Money используется в `Contract.java`

#### Шаг 2: Создание Value Objects (приоритет)
```java
// Пример: Email Value Object
@Embeddable
public class Email {
    private final String value;
    
    private Email(String value) {
        if (!isValid(value)) {
            throw new InvalidEmailException(value);
        }
        this.value = value.toLowerCase();
    }
    
    public static Email of(String value) {
        return new Email(value);
    }
    
    private boolean isValid(String email) {
        // валидация через regex
    }
}
```

#### Шаг 3: Рефакторинг Entity → Aggregate Root
Превратить простые Entity в полноценные аггрегаты:
```java
// До (Anemic Domain Model)
@Entity
public class Contract {
    private Long id;
    private LocalDateTime dateStart;
    // ... геттеры/сеттеры
}

// После (Rich Domain Model)
@Entity
public class Contract {
    @EmbeddedId
    private ContractId id;
    
    private DateRange rentalPeriod;
    private Money totalCost;
    
    // Бизнес-логика в доменном слое
    public void complete() {
        if (!canBeCompleted()) {
            throw new ContractCannotBeCompletedException();
        }
        this.state = RentalState.COMPLETED;
        // Доменное событие
        registerEvent(new ContractCompletedEvent(this.id));
    }
    
    private boolean canBeCompleted() {
        return this.state == RentalState.ACTIVE 
            && LocalDateTime.now().isAfter(this.dateEnd);
    }
}
```

#### Шаг 4: Тестирование
После каждого изменения:
```bash
mvn test
mvn jacoco:report
```
Открыть `target/site/jacoco/index.html` для проверки покрытия

#### Шаг 5: Фиксация прогресса
Обновлять `MIGRATION_PLAN.md` после каждого этапа

---

## 🧪 Тестирование

### Запуск всех тестов
```bash
mvn test
```

### Запуск с отчетом покрытия (JaCoCo)
```bash
mvn clean test jacoco:report
```
Отчет будет доступен: `target/site/jacoco/index.html`

### Текущие метрики
- **Всего тестов**: 1232
- **Пройдено**: 807 (65%)
- **Покрытие кода**: ~50%

### Типы тестов
- **Unit Tests** — тесты репозиториев (`repository/*Test.java`)
- **Integration Tests** — тесты контроллеров (`it/*IntegrationTests.java`)
  - Используют Testcontainers с PostgreSQL
  - Проверяют полный цикл: HTTP → Service → Repository → DB

---

## 🔐 Конфигурация

### База данных (application.yaml)
```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/car_rental
    username: postgres
    password: postgres
```

### JWT настройки
Настройки JWT хранятся в коде (рекомендуется вынести в переменные окружения):
- **Access Token**: действует 30 минут
- **Refresh Token**: действует 7 дней

### CORS
Разрешен доступ с `http://localhost:5176` (фронтенд в dev-режиме)

### Email (Mailpit)
В dev-режиме используется Mailpit:
- **SMTP**: `localhost:1025`
- **Web UI**: `http://localhost:8025`

---

## 📊 Roadmap

### ✅ Реализовано (Q4 2024)
- [x] REST API для автомобилей (CRUD, фильтрация)
- [x] Аутентификация (JWT + Refresh Tokens)
- [x] Регистрация пользователей
- [x] Профиль пользователя
- [x] Управление договорами аренды
- [x] Избранное (favorites)
- [x] Загрузка и управление документами
- [x] Email-уведомления (через Mailpit)
- [x] Аналитика для администраторов
- [x] Docker Compose для dev и prod
- [x] Flyway миграции БД
- [x] Integration и Unit тесты (807 тестов)
- [x] Начало DDD-рефакторинга (Value Object Money)

### 🔄 В разработке (Q1 2025)
- [ ] Завершение DDD-миграции (все Value Objects)
- [ ] Рефакторинг аггрегатов (Contract, Car, Client)
- [ ] Доменные события (Domain Events)
- [ ] Повышение покрытия тестами до 70-80%
- [ ] Исправление падающих тестов (425 шт.)

### 📋 Планируется (Q2 2025)
- [ ] Интеграция с платежными системами
- [ ] Push-уведомления (Firebase)
- [ ] Геолокация автомобилей (GPS tracking)
- [ ] Отзывы и рейтинги автомобилей
- [ ] Система лояльности (бонусы, скидки)
- [ ] Интеграция с MinIO (хранилище файлов)
- [ ] GraphQL API (альтернатива REST)
- [ ] Кэширование (Redis)
- [ ] Мониторинг (Prometheus + Grafana)
- [ ] CI/CD pipeline (GitHub Actions)

### 🚀 Долгосрочные цели (2025+)
- [ ] Микросервисная архитектура (разделение на сервисы)
- [ ] Event-Driven Architecture (Kafka)
- [ ] CQRS (Command Query Responsibility Segregation)
- [ ] Event Sourcing
- [ ] Kubernetes deployment
- [ ] Multi-tenancy (поддержка нескольких каршеринговых компаний)

---

## 🤝 Контрибьюция

### Как внести свой вклад?
1. Fork репозитория
2. Создайте feature-ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### Требования к коду
- Соблюдение Java Code Conventions
- Покрытие тестами (минимум 70% для нового кода)
- Javadoc для публичных методов
- Валидация входных данных
- Обработка исключений

---

## 📝 Лицензия

Проект разработан в образовательных целях (ВГУ, 5 семестр, курс Mobile Development).

---

## 📧 Контакты

Для вопросов и предложений:
- GitHub Issues: [создать issue](../../issues)
- Email: указать ваш email

---

## 🙏 Благодарности

- **Spring Framework** — за отличный фреймворк
- **Testcontainers** — за упрощение интеграционных тестов
- **MapStruct** — за удобный маппинг
- **Lombok** — за сокращение boilerplate кода

---

**Дата последнего обновления**: 4 декабря 2024

**Версия**: 0.0.1-SNAPSHOT

**Статус**: 🔄 В активной разработке

