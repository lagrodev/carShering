# Обзор проекта Car Sharing Backend

## Текущее состояние проекта

### Технологический стек

**Backend:**
- Java 21
- Spring Boot 3.5.6
- Spring Security + JWT (jjwt 0.11.5)
- Spring Data JPA + Hibernate
- PostgreSQL
- Flyway (миграции БД)
- MapStruct (маппинг DTO)
- Lombok

**Инфраструктура:**
- Docker Compose (PostgreSQL, Mailpit, MinIO)
- Maven
- Spring Boot Actuator (базовый мониторинг)
- SpringDoc OpenAPI (Swagger UI)

**Тестирование:**
- JUnit 5 + Spring Boot Test
- Testcontainers (PostgreSQL)
- JaCoCo (покрытие кода с требованиями: 80% instructions, 60% branches)

### Архитектура

**Стиль:** Монолит с переходом на DDD-подход

**Структура по bounded contexts:**
```
src/main/java/org/example/carshering/
├── fleet/              # Управление автопарком (машины, модели, бренды)
│   ├── api/           # REST API слой
│   ├── application/   # Application Services
│   ├── domain/        # Domain Model (model, repository, service, valueobject)
│   └── infrastructure/# Адаптеры (persistence, external services)
├── identity/          # Управление пользователями и документами
│   ├── api/
│   ├── application/
│   ├── domain/
│   └── infrastructure/
├── rental/            # Управление договорами аренды
│   ├── api/
│   ├── application/
│   ├── domain/
│   └── infrastructure/
├── favorites/         # Избранные автомобили (ПЛАНИРУЕТСЯ - Фаза 1.1)
│   └── (пока в service/impl/FavoriteServiceImpl - legacy)
├── authorization/     # Управление ролями и правами доступа (ПЛАНИРУЕТСЯ - Фаза 1.2)
│   └── (пока частично в identity - RoleModel)
├── common/            # Общие компоненты (shared kernel)
└── security/          # Security конфигурация
```

### Реализованная функциональность

**Identity Context (Пользователи):**
- ✅ Регистрация и аутентификация (JWT + Refresh Token)
- ✅ Управление профилем пользователя
- ✅ Управление документами (загрузка, верификация админом)
- ✅ Восстановление пароля через email
- ✅ Верификация email
- ✅ Роли (USER, ADMIN) и авторизация
- ✅ Бан/разбан пользователей

**Fleet Context (Автопарк):**
- ✅ CRUD автомобилей
- ✅ Каталог с фильтрацией (бренд, модель, класс, тип кузова, цена)
- ✅ Управление брендами, моделями, классами авто
- ✅ Избранные автомобили для пользователей
- ✅ Интеграция с MinIO для хранения фото

**Rental Context (Аренда):**
- ✅ Создание договора аренды
- ✅ Статусы договора (CREATED, CONFIRMED, ACTIVE, COMPLETED, CANCELLED)
- ✅ Просмотр контрактов пользователя
- ✅ Отмена контракта (с условиями по срокам)
- ✅ Завершение аренды

**Общие возможности:**
- ✅ REST API с OpenAPI/Swagger документацией
- ✅ Email уведомления (Spring Mail + Mailpit для dev)
- ✅ Миграции БД через Flyway (30+ миграций)
- ✅ CORS настройки для фронтенда
- ✅ Частичные индексы и оптимизация БД

### Прогресс DDD архитектуры

**✅ УЖЕ РЕАЛИЗОВАНО:**

1. **Value Objects (25+ штук):**
   - Common: `CarId`, `ClientId`, `Money`
   - Fleet: `Vin`, `GosNumber`, `Year`, `ImageUrl`, `ImageData`, `FileName`, `DateOfIssue`, `CarStateType`, `IssuingAuthority`
   - Identity: `Email`, `Password`, `Phone`, `Login`, `RoleId`, `RoleName`, `Permission` (enum), `DocumentId`, `DocumentTypeId`, `DocumentNumber`, `DocumentSeries`
   - Rental: `ContractId`, `RentalPeriod`, `RentalStateType`

2. **Domain Models (Aggregates):**
   - Fleet: `CarDomain`, `CarBrandDomain`, `CarClassDomain`, `CarModelDomain`, `CarModelNameDomain`
   - Identity: `Client`, `Document`, `RoleModel`, `DocumentTypeModel`
   - Rental: `Contract`

3. **Role-Based Security:**
   - `RoleModel` с `Set<Permission>` (11 разрешений)
   - Enum `Permission`: VIEW_USERS, MANAGE_USERS, BAN_USERS, VIEW_DOCUMENTS, VERIFY_DOCUMENTS, CREATE_CONTRACTS, UPDATE_CONTRACTS, CANCEL_CONTRACTS, MANAGE_CARS, VIEW_REPORTS, GENERATE_REPORTS

4. **Domain Events (начало):**
   - `EmailVerificationCompletedEvent`, `PasswordResetCompletedEvent`
   - `@EventListener` используется

**⚠️ ТРЕБУЕТ ДОРАБОТКИ:**

1. **Legacy код:**
   - ❌ `FavoriteServiceImpl` в старой структуре `service/impl/` - **ПЛАНИРУЕТСЯ** вынести в отдельный bounded context `favorites/` (Фаза 1.1)
   - ❌ `AnalysisServiceImpl` закомментирован - **ОТЛОЖЕНО** на Фазу 7 (Analytics context)

2. **Новые Bounded Contexts (ПЛАНИРУЮТСЯ):**
   - ❌ `favorites/` - для cross-cutting функциональности избранного (связь Client ↔ Car)
   - ❌ `authorization/` - для RBAC с field-level permissions (сейчас RoleModel в identity)

3. **DDD Patterns:**
   - ❌ `AggregateRoot` интерфейс НЕ создан (domain models его не implement)
   - ❌ Domain Events инфраструктура не завершена (нет `DomainEvent` интерфейса, нет коллекции событий в aggregates)
   - ❌ Repository интерфейсы частично в domain, адаптеры в infrastructure не везде
   - ❌ Domain Service для Roles не создан (планируется для fine-grained RBAC)

### Проблемы и технический долг

**Архитектура:**
- ⚠️ Закомментированные @Service аннотации частично удалены (требуется завершение)
- ⚠️ AggregateRoot pattern не применен
- ⚠️ Бизнес-логика частично в Application Services (должна быть в Domain)

**Тестирование:**
- ⚠️ Тесты существуют (~30 тестовых классов), но некоторые неактуальны
- ⚠️ Отсутствуют интеграционные тесты для критичных сценариев
- ⚠️ Нет нагрузочных тестов
- ⚠️ Нет тестов для security слоя
- ⚠️ Нет тестов для Domain Events

**Код:**
- ⚠️ Значительное количество TODO/FIXME в коде (20+ мест)
- ⚠️ Null values в API responses вместо дефолтных значений

**Инфраструктура:**
- ❌ CI/CD pipeline отсутствует полностью
- ❌ Dockerfile для сборки приложения не создан
- ❌ Нет централизованного логирования (только стандартный SLF4J)
- ❌ Мониторинг базовый (только Actuator, без метрик и алертов)
- ❌ Отсутствует профилирование и трейсинг
- ❌ Нет автоматизированного бэкапа БД

**Безопасность:**
- ⚠️ JWT secret в plain text в application.yaml
- ⚠️ Нет rate limiting для API
- ⚠️ Отсутствует HTTPS конфигурация
- ⚠️ Нет аудита действий пользователей

**Документация:**
- ⚠️ README есть, но неполный
- ⚠️ API документация только через Swagger
- ⚠️ Нет ADR (Architecture Decision Records)
- ⚠️ Отсутствует документация для деплоя

## Основная цель проекта

**Демонстрация компетенций для позиций:**
1. **Backend Developer** — Java/Spring эксперт с знанием DDD, чистой архитектуры, тестирования
2. **DevOps Engineer** — автоматизация, контейнеризация, CI/CD, мониторинг

**Желаемое впечатление работодателя:**
- "Разработчик понимает не только как писать код, но и как его правильно структурировать для роста"
- "Проект может масштабироваться — видна подготовка к микросервисам"
- "Есть culture of quality: тесты, документация, мониторинг"
- "Знает DevOps практики: автоматизация, Docker, CI/CD"
- "Умеет итеративно развивать проект — понятная история коммитов"

## Ключевые приоритеты развития

### 1. Завершение перехода на DDD (высокий приоритет)
- Удалить старые закомментированные сервисы
- Переместить бизнес-логику в domain слой
- Добавить явные Value Objects и Aggregates
- Внедрить Domain Events

### 2. Production-ready качество кода (высокий приоритет)
- Исправить все TODO/FIXME
- Убрать null values из API
- Внедрить централизованную обработку ошибок
- Добавить валидацию на всех уровнях

### 3. Тестирование (высокий приоритет)
- Актуализировать существующие тесты
- Добавить unit-тесты для domain логики
- Создать интеграционные тесты для API
- Contract testing для будущих микросервисов

### 4. CI/CD pipeline (высокий приоритет)
- GitHub Actions для автоматической сборки
- Автоматический запуск тестов
- Code quality checks (SonarQube/Qodana)
- Автоматический деплой в staging

### 5. Observability (средний приоритет)
- Структурированное логирование (JSON)
- Метрики (Micrometer + Prometheus)
- Distributed tracing (OpenTelemetry)
- Health checks и readiness probes

### 6. Безопасность (средний приоритет)
- Externalized configuration (Spring Cloud Config)
- Rate limiting (Bucket4j)
- Security headers
- Dependency scanning

### 7. Контейнеризация (средний приоритет)
- Multi-stage Dockerfile
- Docker Compose для полного окружения
- Оптимизация образов
- Health checks в контейнерах

### 8. Документация (низкий приоритет)
- Развернутый README с диаграммами
- ADR для архитектурных решений
- Runbook для операций
- Swagger улучшения

## Демонстрационная ценность

### Backend компетенции
- **DDD & Clean Architecture**: Четкое разделение на слои, bounded contexts, domain-driven design
- **Spring Boot экспертиза**: Security, Data JPA, Transactions, AOP
- **Database skills**: Flyway миграции, индексы, оптимизация запросов, PostgreSQL специфика
- **Testing**: Unit, Integration, Contract tests с Testcontainers
- **API Design**: RESTful, OpenAPI, версионирование, pagination

### DevOps компетенции
- **Containerization**: Docker, Docker Compose, multi-stage builds
- **CI/CD**: GitHub Actions, automated testing, deployment pipelines
- **Monitoring**: Prometheus, Grafana, logs aggregation
- **Infrastructure as Code**: Docker Compose, возможность расширения до Kubernetes
- **Security**: Secrets management, scanning, HTTPS

### System Design
- **Scalability**: Подготовка к микросервисам через bounded contexts
- **Reliability**: Health checks, graceful shutdown, retry mechanisms
- **Observability**: Logs, metrics, traces — "три столпа"
- **Maintainability**: Чистая структура, документация, тесты

## Путь к микросервисам

**Текущая подготовка:**
- ✅ Bounded contexts уже выделены (fleet, identity, rental)
- ✅ Application services изолированы
- ✅ Domain logic инкапсулирована

**Следующие шаги:**
1. Добавить Contract Testing между контекстами
2. Выделить shared kernel (common events, value objects)
3. Внедрить асинхронную коммуникацию (Spring Events → Kafka)
4. Подготовить каждый контекст к независимому деплою
5. Разделить БД (shared → per-service schema)

**Kafka integration план:**
- Domain Events публикация (ContractCreated, CarRented, etc.)
- Event-driven communication между сервисами
- SAGA pattern для распределенных транзакций
- Event sourcing для audit log

## Итоговая картина

**Сейчас:**
Рабочий монолит с REST API, JWT auth, PostgreSQL, Docker, базовыми тестами и Swagger документацией. Есть переход на DDD, но не завершен. Отсутствуют CI/CD, полноценный мониторинг, production-ready инфраструктура.

**Через 2-3 месяца (целевое состояние):**
Production-ready монолит с:
- Чистой DDD архитектурой
- 80%+ test coverage с актуальными тестами
- CI/CD pipeline (build, test, deploy)
- Полным observability stack (logs, metrics, traces)
- Security best practices
- Качественной документацией
- Готовностью к декомпозиции в микросервисы
- Kafka integration для событий

**Демонстрация работодателю:**
- 📊 Dashboard с метриками в Grafana
- 🔄 CI/CD pipeline с green builds
- 📈 SonarQube/Qodana с высоким рейтингом качества
- 📚 Swagger + подробная документация
- 🎯 Git history с понятными коммитами и инкрементальным развитием
- 🚀 One-command startup (`docker-compose up`)
